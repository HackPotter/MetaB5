using UnityEngine;

public class MockGameContextInitializer : MonoBehaviour
{
    void Awake()
    {
        //GameContext.Initialize(new MockDataProvider(new GameData(), BiologLoadingProcess.UnlockAll));
    }
}

